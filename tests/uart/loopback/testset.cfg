from plptest.testsuite import *

# Called by plptest to declare the tests
def testset_build(testset):

    #
    # Test list decription
    #
    testset.new_make_test('loopback', flags='build_dir_ext=simple')
