from gvtest import *

# Called by gvtest to declare the tests
def testset_build(testset):

    testset.set_name('redmule')

    if testset.get_target().get_name().find('redmule') != -1:
        testset.import_testset(file='gemm/testset.cfg')
