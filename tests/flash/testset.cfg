from plptest import *

# Called by plptest to declare the tests
def testset_build(testset):

    testset.set_name('flash')

    testset.import_testset(file='simple/testset.cfg')
    # This test is not passing, it seems the erase is not working
    #testset.import_testset(file='with_ram/testset.cfg')
