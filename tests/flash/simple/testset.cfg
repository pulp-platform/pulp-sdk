from gvtest.testsuite import *

# Called by gvtest to declare the tests
def testset_build(testset):

    #
    # Test list decription
    #
    test = testset.new_make_test('simple', flags='build_dir_ext=simple')
    if testset.get_target().name in ['siracusa']:
        test.skip('To be fixed')
