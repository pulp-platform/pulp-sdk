from plptest.testsuite import *

# Called by plptest to declare the tests
def testset_build(testset):

    #
    # Test list decription
    #
    test = testset.new_make_test('with_ram', flags='build_dir_ext=with_ram')
    test.skip('Erase seems to not be supported on gvsoc')
