from plptest.testsuite import *

# Called by plptest to declare the tests
def testset_build(testset):

    #
    # Test list decription
    #
    testset.new_make_test('read:fc', flags='build_dir_ext=read_fc')
    testset.new_make_test('read:cl', flags='build_dir_ext=read_cl USE_CLUSTER=1')
